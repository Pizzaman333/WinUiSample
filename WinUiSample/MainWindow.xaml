<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinUiSample.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUiSample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WinUiSample">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Background colors for rows -->
        <Rectangle Grid.Row="0" Grid.ColumnSpan="3" Fill="LightBlue"/>
        <Rectangle x:Name="SpecialBoy"  Grid.Row="1" Grid.ColumnSpan="3" Fill="LightGreen"/>
        <Rectangle Grid.Row="2" Grid.ColumnSpan="3" Fill="LightCoral"/>
        <!-- Transparent rectangles for columns -->
        <Rectangle Grid.Column="0" Grid.RowSpan="3" Fill="LightYellow" Opacity="0.5"/>
        <Rectangle Grid.Column="1" Grid.RowSpan="3" Fill="LightPink" Opacity="0.5"/>
        <Rectangle Grid.Column="2" Grid.RowSpan="3" Fill="LightBlue" Opacity="0.5"/>

        <Button  
            x:Name="OpenButton" 
            Click="OpenButton_Click" 
            Grid.Column="1"
            Grid.Row="1"
            HorizontalAlignment="Center" 
            VerticalAlignment="Center">
            I'm a button, aren't I?
        </Button>
    </Grid>

</Window>
